{
  "name": "wola",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "test:isolation": "node --import dotenv/config --test packages/db/test/*.test.mjs"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
